<h1><%= @category.name %></h1>

<p><%= @category.description %></p>

<li><%= link_to "Edit", edit_category_path(@category) %></li>
<li><%= link_to "Delete", @category, data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li>

</ul>